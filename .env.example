DATABASE_URL=
PORT=3001
ADMIN_PASSCODE=

# NotebookLM import (/admin/import) uses fetch only — no Playwright install required.
# Optional local CLI: npx playwright install chromium && node scripts/extract-notebooklm-quiz.js <url>
